# Agent / contributor entry point

The **full autonomous build spec** for iteration 1 (paper, figures, analysis, audio) lives here:

**[`first/AGENTS.md`](first/AGENTS.md)**

All iteration-1 code, LaTeX, WAVs, and the theory PDF are under **`first/`** (see root [`README.md`](README.md)).

**Environment:** from repo root, run [`scripts/bootstrap_env.sh`](scripts/bootstrap_env.sh) to create `.venv` and install `first/code/requirements.txt` plus `requirements-jupyter.txt`.
